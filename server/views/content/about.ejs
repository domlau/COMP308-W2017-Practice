    <% include ../partials/header.ejs %>
    
    <!-- ABOUT CONTENT -->
    <div class="container">
      <div class="row">
        <div class="col-md-offset-3 col-md-6">
          <h1><%= title %></h1>
          <p>Welcome to <%= title %></p>
          <br>
          <h3>List of names</h3>
          <ul>
            <% for (let count = 0; count < practice.length; count++) { %>
              <li class="list-group-item">
                <%= practice[count].name %>
              </li>
            <% } %>
          </ul>
        </div>
      </div>
    </div>

    <% include ../partials/footer.ejs %>